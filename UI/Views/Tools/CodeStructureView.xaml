<UserControl x:Class="Skriptorium.UI.Views.Tools.CodeStructureView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid>
        <TabControl>
            <!-- Tab für Instanzen -->
            <TabItem Header="{DynamicResource Instances}">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontWeight="Bold" FontSize="14"/>
                    </DataTemplate>
                </TabItem.HeaderTemplate>
                <DataGrid x:Name="InstancesGrid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" MouseDoubleClick="InstancesGrid_MouseDoubleClick">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{DynamicResource Name}" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="{DynamicResource Line}" Binding="{Binding Line}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <!-- Tab für Funktionen -->
            <TabItem Header="{DynamicResource Functions}">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontWeight="Bold" FontSize="14" />
                    </DataTemplate>
                </TabItem.HeaderTemplate>
                <DataGrid x:Name="FunctionsGrid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" MouseDoubleClick="FunctionsGrid_MouseDoubleClick">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{DynamicResource Type}" Binding="{Binding ReturnType}" Width="100"/>
                        <DataGridTextColumn Header="{DynamicResource Name}" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="{DynamicResource Line}" Binding="{Binding Line}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <!-- Tab für Variablen -->
            <TabItem Header="{DynamicResource Variables}">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontWeight="Bold" FontSize="14" />
                    </DataTemplate>
                </TabItem.HeaderTemplate>
                <DataGrid x:Name="VariablesGrid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" MouseDoubleClick="VariablesGrid_MouseDoubleClick">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{DynamicResource Name}" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="{DynamicResource Type}" Binding="{Binding TypeName}" Width="*"/>
                        <DataGridTextColumn Header="{DynamicResource Line}" Binding="{Binding Line}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <!-- Tab für Konstanten -->
            <TabItem Header="{DynamicResource Constants}">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontWeight="Bold" FontSize="14" />
                    </DataTemplate>
                </TabItem.HeaderTemplate>
                <DataGrid x:Name="ConstantsGrid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" MouseDoubleClick="ConstantsGrid_MouseDoubleClick">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{DynamicResource Name}" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="{DynamicResource Type}" Binding="{Binding TypeName}" Width="*"/>
                        <DataGridTextColumn Header="{DynamicResource Value}" Binding="{Binding Value}" Width="*"/>
                        <DataGridTextColumn Header="{DynamicResource Line}" Binding="{Binding Line}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>